# =====================================================
# 환경 변수 설정 파일
# =====================================================
# 이 파일을 .env로 복사한 후 서버 환경에 맞게 수정하세요
# cp .env.example .env
# vi .env

# =====================================================
# Ollama 설정 (서버의 기존 Ollama Docker 컨테이너)
# =====================================================
# 서버에서 실행 중인 Ollama 컨테이너의 주소로 변경
# 예: http://ollama-container-name:11434
# 예: http://host.docker.internal:11434 (Docker Desktop)
# 예: http://172.17.0.1:11434 (Docker 호스트)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b

# =====================================================
# PostgreSQL 데이터베이스 설정 (서버의 기존 PostgreSQL)
# =====================================================
# 형식: postgresql://user:password@host:port/dbname
# 서버에서 실행 중인 PostgreSQL 컨테이너의 정보로 변경
# 예: postgresql://admin:admin123@postgres-container-name:5432/applicants_db
# 예: postgresql://admin:admin123@host.docker.internal:5432/applicants_db
# 예: postgresql://admin:admin123@172.17.0.1:5432/applicants_db
DATABASE_URL=postgresql://admin:admin123@localhost:5432/applicants_db

# =====================================================
# 데이터베이스 스키마 설정
# =====================================================
# 테이블이 속한 스키마 이름 (기본값: public)
# 서버 데이터베이스에서 다른 스키마를 사용하는 경우 변경
# 예: hr_schema, applicant_schema 등
# 참고: backend/app/models/applicant.py의 __table_args__ 수정 필요
# DB_SCHEMA=public
